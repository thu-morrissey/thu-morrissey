name: Generate Snake Animation

on:
  schedule:
    - cron: "0 */12 * * *"   # every 12 hours
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake.gif?color_snake=purple&color_dots=#0d1117,#01311f,#034525,#0f6d31,#00c647&color_background=#0d1117

      - name: Debug outputs string (make sure & is not encoded)
        run: |
          echo "INPUT_OUTPUTS from environment:"
          echo "${INPUT_OUTPUTS}"
        env:
          INPUT_OUTPUTS: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake.gif?color_snake=purple&color_dots=#0d1117,#01311f,#034525,#0f6d31,#00c647&color_background=#0d1117
                 
      - name: Upload animation artifacts
        uses: actions/upload-artifact@v4
        with:
          name: snake-animations
          path: dist/

      - name: Deploy to GitHub Pages
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
